
import './globals.css';
export const metadata = { title: "Our Social Image — AI Growth Hub" };

export default function RootLayout({ children }) {
  return (
    <html lang="en"><body className="min-h-screen">{children}</body></html>
  );
}
